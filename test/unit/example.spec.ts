import { describe, expect, it } from 'vitest';

// TODO: Delete this file, if a unit test exists
describe('example', () => {
  it('adds correctly', () => {
    const add = (a: number, b: number): number => a + b;
    expect(add(2, 3)).toBe(5);
  });
});
